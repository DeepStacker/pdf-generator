name: Build Platform Binaries
on:
  push:
    tags:
      - "v*"
permissions:
  contents: write
jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: pip-win-${{ hashFiles('requirements.txt') }}
          restore-keys: pip-win-
      - uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pyinstaller
          key: pyi-win-${{ hashFiles('pdf_generator.spec') }}
          restore-keys: pyi-win-
      - run: pip install pyinstaller reportlab openpyxl pandas
      - run: pyinstaller --noconfirm pdf_generator.spec
      - shell: pwsh
        env:
          TAG: ${{ github.ref_name }}
        run: |
          Compress-Archive -Path dist -DestinationPath "Audit_Engine_${env:TAG}_binary_windows.zip"
      - shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          gh release view $env:TAG 2>$null
          if ($LASTEXITCODE -ne 0) { gh release create $env:TAG --title $env:TAG --notes "Windows binary" }
          gh release upload $env:TAG "Audit_Engine_${env:TAG}_binary_windows.zip" --clobber

  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-linux-${{ hashFiles('requirements.txt') }}
          restore-keys: pip-linux-
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pyinstaller
          key: pyi-linux-${{ hashFiles('pdf_generator.spec') }}
          restore-keys: pyi-linux-
      - run: pip install pyinstaller reportlab openpyxl pandas
      - run: pyinstaller --noconfirm pdf_generator.spec
      - env:
          TAG: ${{ github.ref_name }}
        run: |
          cd dist && zip -r "../Audit_Engine_${TAG}_binary_linux.zip" .
      - env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ github.ref_name }}"
          ZIP="Audit_Engine_${TAG}_binary_linux.zip"
          gh release view "$TAG" 2>/dev/null || gh release create "$TAG" --title "$TAG" --notes "Linux binary"
          gh release upload "$TAG" "$ZIP" --clobber
